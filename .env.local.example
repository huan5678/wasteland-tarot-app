# ============================================
# Local Development Environment Configuration
# 本地開發環境配置
# ============================================
# 複製此檔案為 .env.local 並填入實際值

# ============================================
# API Configuration (本地開發 - 直接後端呼叫)
# ============================================
# 本地開發使用直接後端呼叫 (Direct Backend Access)
# 原因：Next.js API Route 在 localhost 跨 port 時無法正確設定 httpOnly cookies
#
# ⚠️ Zeabur 生產環境必須改用 API Route Proxy（設為空字串）
# 生產環境範例：NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ============================================
# Website Configuration
# ============================================
# 開發環境使用 localhost
# 生產環境範例：NEXT_PUBLIC_SITE_URL=https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================
# Supabase Configuration
# ============================================
# 開發環境測試用配置
# 生產環境必須填入實際的 Supabase 專案配置
NEXT_PUBLIC_SUPABASE_URL=https://test.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key-for-frontend-testing-12345

# ============================================
# Feature Flags (功能開關)
# ============================================
# Daily Bingo 配置
NEXT_PUBLIC_BINGO_ENABLE=true
NEXT_PUBLIC_BINGO_CYCLE_LENGTH=25
NEXT_PUBLIC_BINGO_MIN_LINES_FOR_REWARD=3

# 功能開關
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_WEB_AUDIO=true
NEXT_PUBLIC_ENABLE_DOTO_FONT=true

# 開發模式配置
NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_MOCK_API=false

# ============================================
# 🚀 Production Deployment Notes (Zeabur)
# ============================================
# 生產環境主要差異：
# 1. NEXT_PUBLIC_API_BASE_URL= (空字串，使用 API Route Proxy)
# 2. NEXT_PUBLIC_SITE_URL=https://yourdomain.com
# 3. NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# 4. NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-key
#
# 參考 docs/DEPLOYMENT.md 了解完整部署流程
